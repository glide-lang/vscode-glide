{
  "name": "glide",
  "displayName": "Glide",
  "description": "Glide language support: syntax highlighting + LSP (diagnostics, hover, goto, completion, rename, refs).",
  "version": "0.3.2",
  "publisher": "glide-lang",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/glide-lang/vscode-glide.git"
  },
  "bugs": {
    "url": "https://github.com/glide-lang/vscode-glide/issues"
  },
  "homepage": "https://github.com/glide-lang/vscode-glide#readme",
  "keywords": ["glide", "language-server", "lsp", "systems", "compiler"],
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": ["Programming Languages"],
  "icon": "icons/glide-256.png",
  "main": "./out/extension.js",
  "activationEvents": [
    "onLanguage:glide"
  ],
  "contributes": {
    "languages": [
      {
        "id": "glide",
        "aliases": ["Glide", "glide"],
        "extensions": [".glide"],
        "configuration": "./language-configuration.json",
        "icon": {
          "light": "./icons/glide-light.png",
          "dark": "./icons/glide-dark.png"
        }
      }
    ],
    "grammars": [
      {
        "language": "glide",
        "scopeName": "source.glide",
        "path": "./syntaxes/glide.tmLanguage.json"
      }
    ],
    "configuration": {
      "title": "Glide",
      "properties": {
        "glide.path": {
          "type": "string",
          "default": "glide",
          "description": "Path to the `glide` executable. If left as `glide`, the extension looks on PATH."
        },
        "glide.trace.server": {
          "type": "string",
          "enum": ["off", "messages", "verbose"],
          "default": "off",
          "description": "Trace LSP messages between VSCode and the Glide server."
        }
      }
    }
  },
  "scripts": {
    "compile": "tsc -p .",
    "watch": "tsc -w -p ."
  },
  "dependencies": {
    "vscode-languageclient": "^9.0.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.85.0",
    "typescript": "^5.3.0"
  }
}
